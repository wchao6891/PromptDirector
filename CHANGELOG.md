# Changelog

## 1.18.16

- 统一创作 Skill、精选 Skill 与创作台参考区的分段导航和卡片密度，去除多余空白、整卡高亮与页面间的尺寸漂移。
- 精选 Skill 列表只突出名称、用途和保存操作；版本与许可移入详情折叠区，重复的正文标题不再重复显示。
- 精选 Skill 支持从列表直接安全保存，并明确区分首次保存、同版已保存、新版本另存与校验过程。

## 1.18.15

- 精选 Skill 目录与投稿保留用户可读调用名，稳定公开 ID 继续独立用于版本识别；保存到本地后不再显示内部 `creative-skill-*` 编号。

## 1.18.14

- 精选 Skill 新增独立目录、手动投稿与本地保存流程；投稿只需公开署名、自动摘要和开放确认，维护字段移到人工审核阶段。
- 精选案例与精选 Skill 统一深浅色、导航、按钮和空态；公共目录先行上线，空目录不再误报 404。
- 图片分析支持把动态发现的模型手动分配给 DeepSeek；修复 JSON 视觉 Schema 断链、空响应原因不明以及 `OCR` 根字段大小写漂移。
- 跨设备备份改为严格手动执行，未变化时零写入，重复启动合并为同一任务，取消后停止并回滚未完成写入。

## 1.18.13

- 项目排序时不再误选文字，案例选择期间排序入口保持原位并以禁用状态表达互斥关系。
- 快速笔记、视频链接和导入确认的项目组合框会根据弹窗可用空间上下展开，不再被滚动区或底部操作栏裁切。
- 案例排序与选择入口改为轻量工具条；案例删除入口移到来源信息旁，无来源案例仍在详情正文底部提供回收站操作。

## 1.18.12

- 项目排序改为显式管理态下整行拖动，支持触摸与键盘移动，并在保存失败时恢复原顺序；项目菜单同步缩短并去除省略文案。
- 批量选择栏只保留一个已选计数，详情删除操作移到内容末尾，单图案例不再显示共享提示词编辑入口，标题保存改为同行操作。
- 快速笔记、视频链接和导入确认复用同一可创建项目组合框；精选案例排序回归公共主题 Select，清除残留的系统白灰菜单。
- “补全资料索引”明确说明本地分类、色卡和相似匹配用途，按检查、补全与完成状态显示真实文字，不再使用无反馈的常驻进度条。

## 1.18.11

- 统一 Chrome 134+ 下拉菜单视觉，保留原生键盘、表单与无障碍行为。
- 详情、外部采集和导入确认统一使用多标签编辑器；界面术语收敛为“添加标签”和“AI 标签”，层级标签默认只显示叶子名称。
- 批量栏恢复稳定居中，项目排序删除常驻下拉并改为按需上下移动，案例选择与排序完成使用不同图标。
- 回收站新增真正的“全部恢复”，桌面项目侧栏支持拖动与键盘调整宽度并记住偏好。

## 1.18.10

- 案例详情改用紧凑的多项目下拉，自由标签与回收站操作直接可用，不再被冗余说明和异常复选框布局干扰。
- 修复全选筛选结果后无法清空的问题；案例与项目排序改为显式进入管理态，完成后立即隐藏移动控件。
- 侧栏只保留项目与回收站，回收站增加素材封面、完整标题和图标操作。
- 更新提醒只在真实存在新版本时强调可执行动作，当前已是最新版时保持中性。

## 1.18.9

- 分享包把 Chrome Web Store 设为普通用户的主安装入口，并保留源码仓库作为次级入口。
- README 的普通用户安装说明改为商店安装与自动更新；GitHub Release 本地包只用于源码检查和开发验证。
- AI 设置改为“任务默认”和“服务连接”分页，服务保存或模型刷新后会在当前窗口立即同步连接状态、模型目录和任务能力。
- 服务配置存在未保存更改时，点击遮罩不再关闭；关闭、取消或 Escape 会先要求确认放弃。
- 修复案例库图片墙最后一列和焦点框越出容器的问题，并加强缩放、追加与图片加载后的稳定布局。
- 智能选图优先真实图片、视频和画布，去除同时包住提示词与按钮的重复外层候选，并按背景图实际绘制区域计算边界。
- 网页采集会预检当前站点权限，按当前来源授权后在同一次操作中继续扫描，并分别提示拒绝授权、站点访问限制、不可注入页面与脚本启动失败。
- 修复侧栏在工具栏图标已授予临时访问权后仍误判当前页不可访问的问题；授权状态会在同一次采集操作中重新读取并继续申请当前网站权限。
- 工具栏图标改为只打开或保持侧栏，不再把已打开的侧栏切换关闭；Chrome 新标签页等内部页面会明确说明不可采集，普通网页仍可继续智能选图。

## 1.18.8

- 修复完整资料夹备份中的 RTF 文档恢复：旧备份即使以 `.bin` 保存，也会通过真实文件签名确认后安全恢复。
- 新备份使用 `.rtf` 扩展名，并在浏览器未提供文件类型时沿用已保存的文档类型。
- 损坏或伪装成 RTF 的文件仍会在写入资料库前被拒绝。

## 1.18.7

- 锁定 Chrome Web Store 正式扩展身份；正式打包先用项目公钥校验目标 ID，再从商店上传清单中移除仅供本地开发使用的 `key` 字段。
- 对齐中英文商店品牌名，移除超限的英文短名称，并把可选 DeepSeek 域名访问改为运行时请求。
- 补齐固定精选目录域名、Limited Use 和商店隐私字段说明，增加可重复执行的商店材料检查。
- 重建当前品牌图、宣传图和三张隔离生成的真实扩展截图。
- 修复完整浏览器回归中的图片加载、后台状态同步和扩展页刷新竞态。

## 1.18.5

- 重做网页采集：先确认创作主体，再检查正文与媒体；同一列表批量采集不会进入详情页，也不会把未确认媒体静默写入案例。
- 完整文章按原网页顺序保留标题、段落、列表、引用、代码、表格及相关媒体；安全文档可保存本地副本，未知文件只保留来源链接。
- 精选案例恢复主资料库式浏览、详情查看、视频播放和选择性保存，保存内容真正进入个人本地资料库。
- 创作台“参考”数量只统计案例；有原提示词或有效画面分析时，可完全不读图、不分析图片并以零图片载荷生成。
- 修复生成完成后按钮仍显示运行状态的问题，并补充刷新竞态保护，完成后可以立即开始下一轮。
- 参考图参与方式及其 token 影响已补齐中英文说明。

## 1.18.4

PromptDirector 从这个经过开源边界清理和完整验证的版本开始维护公开历史。

- 本地优先保存和整理文字、图片、视频、文档、提示词与创作 Skill。
- 支持图片和视频参考、视觉分析、创作规划、媒体生成与结果版本记录。
- 提供资料夹备份、加密同步、分享包、精选案例和可移植 Skill 工作流。
- 外部 AI 请求由用户主动发起，并明确展示服务、模型、发送范围与付费确认。
- 公开源码、测试和发布包可以在标准项目环境中独立验证。

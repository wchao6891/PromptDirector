# Changelog

## 1.19.6

- AI 服务连接恢复展示当前账号目录中的全部可选分析模型；用户明确选择后才联动兼容的空缺任务，已有手动任务路由继续保持不变。
- 创作台模型菜单恢复展示所有已连接的创作规划候选；新会话仍以“创作规划”任务路由为默认，本轮切换只更新当前会话，并按实际选择的服务、模型和凭据发送。
- 图片生成与图片分析路由彻底解耦：已配置的独立生图服务不再因图片分析未分配而被禁用，xAI 图片分析模型也不会被误当成图片生成模型。
- 固定 ID 本地扩展实验室新增多模型路由回归场景，长期锁定连接页多模型、创作台跨服务切换和独立图片生成三条行为。

## 1.19.5

- AI 服务保持一次连接，任务路由直接展示文字、Skill、创作规划、图片与视频实际调用的服务和模型；新连接只补齐空缺或仍由连接管理的路由，不覆盖用户手动分配。
- 案例详情右侧统一提供视频逆推、创意拆解、广告审片和自定义问题；每个入口直接编辑本次指令并一次发送，逆推结果保存在对应视频的 AI 提示词中，可选在同一次请求生成视觉标签且不覆盖原始提示词。
- 图片与视频批量分析保持独立入口；视频批量默认并发 2，汇总展示并确认缺失文件、不兼容来源和正在分析的排除数量与原因，后台恢复不会自动重发状态未知的付费请求。
- 创作台和 Skill 按具体素材分别使用原始提示词、当前 AI 视觉逆推与人工时间笔记；原始内容优先且不会被 AI 结果或其他视频的内容覆盖。

## 1.19.4

- 新增智谱 GLM 服务连接和按任务模型分配，动态读取账号模型目录；GLM-5.3-Flash 可用于文字标签、Skill 提炼、创作规划、图片分析与视频分析，未确认能力的模型不会被静默代替。
- 案例库本地视频可主动发送给 GLM-5.3-Flash 分析：发送前明确展示本地文件范围并使用原始 Base64；无法编码的格式或只有社交平台播放页时，会在付费请求前停止并说明原因，未公开的服务端大小边界不会被客户端擅自写死。
- 创作台可选择已分配的智谱规划模型，并按具体模型分别发送 Data URL 或原始 Base64 参考图；不再展示模型没有提供的思考开关，也不会在刷新、失败或媒体生成阶段切换到其他规划服务。
- AI 服务错误统一显示实际服务名称并脱敏密钥；本地扩展实验室会阻止未声明的外网请求，验证固定扩展身份、案例库、采集器、创作台、升级与可恢复状态，可长期用于发布前自动化验收。

## 1.19.3

- ZIP 分享包、完整资料夹备份与手动跨设备同步共用同一套媒体格式、案例身份、项目关系和清理规则，覆盖音频、动图、现代图片、字幕、视频与文档等可携带资源。
- 单个缺失、损坏、类型错误或结构不兼容的资源只隔离自身，其他健康案例继续导入、恢复或同步；预检会列出被跳过的案例和文件，提交后的资料库不保留悬空媒体、损坏 AI 标签或待修复占位。
- 已标记完成的备份若后来发生局部资源损坏，会明确降级为救援恢复并禁止精确替换；未知解析错误、未来数据版本和无法证明逐项损失的异常仍会停止，避免静默吞掉资料。
- 完整导入采用预检后单次事务提交，支持安全合并、精确替换、自动恢复点与失败回滚；同步在业务提交后才清理旧媒体，存储读取失败时不会写入半成品。
- 保留既有用户数据的正式迁移、语义去重和多项目归属，继续用 1.19.2 正式版本夹具验证升级与完整资料往返。

## 1.19.2

- 历史分享包统一经过 v1–v5 迁移后再导入，兼容 `jpeg/jpg` 等同族格式；无效 AI 标签会被丢弃并记录，不再阻断仍可恢复的案例。
- 分享 ZIP 按案例和媒体隔离失败：单个缺失、损坏、类型不符或描述不安全的文件会被跳过，其他正确案例继续导入；导入确认会明确列出保留、跳过和丢弃数量。
- 导入写入改为可重复确认的事务流程，网络回执丢失不会造成重复案例或孤立媒体；完全相同的案例与媒体只跳过精确副本，不自动合并用户主动保存的相似内容。
- 完整资料夹备份升级为可换电脑恢复：包含回收站案例和项目，并把仍可读取的本机链接原件复制进备份；写完后会回读自检，任一核心素材不可读时不写完成标记。
- 修复项目删除与恢复关系、历史标签树在回收站中的迁移，以及坏 AI 标签随回收站案例或媒体复活的问题。
- 创作台可复用带有效画面描述的旧版图片分析；图片分析改为可观察的后台任务，关闭窗口只断开本页，显式停止与再次分析分别处理，并让交互任务优先于后台导入分析。
- 历史迁移继续保留分析请求次数和规范化诊断；无法由 Chrome 预览的本地 MPEG-4 视频在案例墙明确显示为本地文件，不再伪装成外部“视频来源”。

## 1.19.1

- 修复项目侧栏与案例管理两处批量画面分析入口：选择范围、开始按钮、运行中进度、暂停与失败数量都会即时显示，旧任务也不会再用过期模型覆盖当前设置。
- 画面分析改为原子保存：只有同时得到详细反推提示词与有效 AI 标签才算成功；缺字段、空标签和旧版半成品统一明确失败，不再写入不完整案例。
- 反推提示词继续覆盖构图、主体、空间、镜头、光线、色彩、材质、风格、文字与画面关系等复刻维度；对兼容视觉服务默认关闭思考，并收敛为“提示词 + 标签”的结构化输出。
- 分析任务使用统一的受控并发与后台恢复机制，区分网络、限流、服务端和格式错误；每张图的网络重试与格式纠正共享最多 3 次真实请求预算，界面提供可重试的中文结果。
- 文字批量分析改为以实际保存的检索标签判定成功，并与案例当前主图提示词使用同一份分析输入，避免显示成功却在详情页看不到标签。
- 分享包与完整资料库导入会校验并重建 AI 标签引用；即使图片 ID 冲突并被重命名，视觉标签也会跟随对应图片迁移，缺失词表则停止导入并说明问题。
- 案例详情页移除重复的下方反推提示词模块，只保留一处可编辑提示词入口；本地索引维护也不再使用容易与 AI 失败混淆的“待补全”文案。

## 1.19.0

- 项目升级为可嵌套的树形结构：支持空父项目、跨层级拖动、键盘管理、子树去重统计，并无损迁移原有项目、案例归属与顺序。
- 项目内容区会先展示直接子项目，再展示直接案例；分享、批量分析、Composer 与 Skill 来源默认覆盖完整子树。
- 案例勾选管理的“项目”菜单新增批量移出，只解除目标项目关系，不删除案例或影响其他项目。
- 项目树已覆盖回收站恢复、新旧分享包、精选导入与手动同步；完整路径选择始终使用项目 ID，不再依赖可能重名的显示文字。
- 批量 AI 分析改为可并发、可后台恢复的持久任务，修复多结果提交丢失、异常高频重试和中断后不继续执行的问题。

## 1.18.16

- 统一创作 Skill、精选 Skill 与创作台参考区的分段导航和卡片密度，去除多余空白、整卡高亮与页面间的尺寸漂移。
- 精选 Skill 列表只突出名称、用途和保存操作；版本与许可移入详情折叠区，重复的正文标题不再重复显示。
- 精选 Skill 支持从列表直接安全保存，并明确区分首次保存、同版已保存、新版本另存与校验过程。

## 1.18.15

- 精选 Skill 目录与投稿保留用户可读调用名，稳定公开 ID 继续独立用于版本识别；保存到本地后不再显示内部 `creative-skill-*` 编号。

## 1.18.14

- 精选 Skill 新增独立目录、手动投稿与本地保存流程；投稿只需公开署名、自动摘要和开放确认，维护字段移到人工审核阶段。
- 精选案例与精选 Skill 统一深浅色、导航、按钮和空态；公共目录先行上线，空目录不再误报 404。
- 图片分析支持把动态发现的模型手动分配给 DeepSeek；修复 JSON 视觉 Schema 断链、空响应原因不明以及 `OCR` 根字段大小写漂移。
- 跨设备备份改为严格手动执行，未变化时零写入，重复启动合并为同一任务，取消后停止并回滚未完成写入。

## 1.18.13

- 项目排序时不再误选文字，案例选择期间排序入口保持原位并以禁用状态表达互斥关系。
- 快速笔记、视频链接和导入确认的项目组合框会根据弹窗可用空间上下展开，不再被滚动区或底部操作栏裁切。
- 案例排序与选择入口改为轻量工具条；案例删除入口移到来源信息旁，无来源案例仍在详情正文底部提供回收站操作。

## 1.18.12

- 项目排序改为显式管理态下整行拖动，支持触摸与键盘移动，并在保存失败时恢复原顺序；项目菜单同步缩短并去除省略文案。
- 批量选择栏只保留一个已选计数，详情删除操作移到内容末尾，单图案例不再显示共享提示词编辑入口，标题保存改为同行操作。
- 快速笔记、视频链接和导入确认复用同一可创建项目组合框；精选案例排序回归公共主题 Select，清除残留的系统白灰菜单。
- “补全资料索引”明确说明本地分类、色卡和相似匹配用途，按检查、补全与完成状态显示真实文字，不再使用无反馈的常驻进度条。

## 1.18.11

- 统一 Chrome 134+ 下拉菜单视觉，保留原生键盘、表单与无障碍行为。
- 详情、外部采集和导入确认统一使用多标签编辑器；界面术语收敛为“添加标签”和“AI 标签”，层级标签默认只显示叶子名称。
- 批量栏恢复稳定居中，项目排序删除常驻下拉并改为按需上下移动，案例选择与排序完成使用不同图标。
- 回收站新增真正的“全部恢复”，桌面项目侧栏支持拖动与键盘调整宽度并记住偏好。

## 1.18.10

- 案例详情改用紧凑的多项目下拉，自由标签与回收站操作直接可用，不再被冗余说明和异常复选框布局干扰。
- 修复全选筛选结果后无法清空的问题；案例与项目排序改为显式进入管理态，完成后立即隐藏移动控件。
- 侧栏只保留项目与回收站，回收站增加素材封面、完整标题和图标操作。
- 更新提醒只在真实存在新版本时强调可执行动作，当前已是最新版时保持中性。

## 1.18.9

- 分享包把 Chrome Web Store 设为普通用户的主安装入口，并保留源码仓库作为次级入口。
- README 的普通用户安装说明改为商店安装与自动更新；GitHub Release 本地包只用于源码检查和开发验证。
- AI 设置改为“任务默认”和“服务连接”分页，服务保存或模型刷新后会在当前窗口立即同步连接状态、模型目录和任务能力。
- 服务配置存在未保存更改时，点击遮罩不再关闭；关闭、取消或 Escape 会先要求确认放弃。
- 修复案例库图片墙最后一列和焦点框越出容器的问题，并加强缩放、追加与图片加载后的稳定布局。
- 智能选图优先真实图片、视频和画布，去除同时包住提示词与按钮的重复外层候选，并按背景图实际绘制区域计算边界。
- 网页采集会预检当前站点权限，按当前来源授权后在同一次操作中继续扫描，并分别提示拒绝授权、站点访问限制、不可注入页面与脚本启动失败。
- 修复侧栏在工具栏图标已授予临时访问权后仍误判当前页不可访问的问题；授权状态会在同一次采集操作中重新读取并继续申请当前网站权限。
- 工具栏图标改为只打开或保持侧栏，不再把已打开的侧栏切换关闭；Chrome 新标签页等内部页面会明确说明不可采集，普通网页仍可继续智能选图。

## 1.18.8

- 修复完整资料夹备份中的 RTF 文档恢复：旧备份即使以 `.bin` 保存，也会通过真实文件签名确认后安全恢复。
- 新备份使用 `.rtf` 扩展名，并在浏览器未提供文件类型时沿用已保存的文档类型。
- 损坏或伪装成 RTF 的文件仍会在写入资料库前被拒绝。

## 1.18.7

- 锁定 Chrome Web Store 正式扩展身份；正式打包先用项目公钥校验目标 ID，再从商店上传清单中移除仅供本地开发使用的 `key` 字段。
- 对齐中英文商店品牌名，移除超限的英文短名称，并把可选 DeepSeek 域名访问改为运行时请求。
- 补齐固定精选目录域名、Limited Use 和商店隐私字段说明，增加可重复执行的商店材料检查。
- 重建当前品牌图、宣传图和三张隔离生成的真实扩展截图。
- 修复完整浏览器回归中的图片加载、后台状态同步和扩展页刷新竞态。

## 1.18.5

- 重做网页采集：先确认创作主体，再检查正文与媒体；同一列表批量采集不会进入详情页，也不会把未确认媒体静默写入案例。
- 完整文章按原网页顺序保留标题、段落、列表、引用、代码、表格及相关媒体；安全文档可保存本地副本，未知文件只保留来源链接。
- 精选案例恢复主资料库式浏览、详情查看、视频播放和选择性保存，保存内容真正进入个人本地资料库。
- 创作台“参考”数量只统计案例；有原提示词或有效画面分析时，可完全不读图、不分析图片并以零图片载荷生成。
- 修复生成完成后按钮仍显示运行状态的问题，并补充刷新竞态保护，完成后可以立即开始下一轮。
- 参考图参与方式及其 token 影响已补齐中英文说明。

## 1.18.4

PromptDirector 从这个经过开源边界清理和完整验证的版本开始维护公开历史。

- 本地优先保存和整理文字、图片、视频、文档、提示词与创作 Skill。
- 支持图片和视频参考、视觉分析、创作规划、媒体生成与结果版本记录。
- 提供资料夹备份、加密同步、分享包、精选案例和可移植 Skill 工作流。
- 外部 AI 请求由用户主动发起，并明确展示服务、模型、发送范围与付费确认。
- 公开源码、测试和发布包可以在标准项目环境中独立验证。
